---
phase: 05-distribution-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .github/workflows/release.yml
autonomous: true

must_haves:
  truths:
    - "README has a dedicated Installation section with download, standard install, and Sequoia-specific steps"
    - "README documents xattr -cr command with explanation of what it does and why"
    - "README includes optional checksum verification instructions"
    - "README explains why the app is unsigned"
    - "xattr command is consistently xattr -cr across README and release notes"
    - "First release v1.1.0 is triggered and downloadable from GitHub Releases"
  artifacts:
    - path: "README.md"
      provides: "Comprehensive Installation section covering standard and Sequoia paths"
      contains: "xattr -cr"
    - path: ".github/workflows/release.yml"
      provides: "Release notes with consistent xattr -cr command"
      contains: "xattr -cr"
  key_links:
    - from: "README.md"
      to: "GitHub Releases page"
      via: "Link to releases page"
      pattern: "github.com/bacilo/pomodoro-mac/releases"
    - from: "README.md"
      to: ".github/workflows/release.yml"
      via: "Consistent xattr command"
      pattern: "xattr -cr"
---

<objective>
Update README.md with comprehensive installation documentation covering standard and Sequoia-specific paths, align xattr command usage across README and release workflow, and trigger the first v1.1.0 release.

Purpose: Users downloading the app need clear, copy-paste-ready instructions to install it successfully, especially on macOS Sequoia 15.1+ where unsigned apps face severe restrictions. The xattr -cr command must be documented consistently across all user-facing surfaces.

Output: Updated README.md with Installation section, updated release workflow with consistent xattr command, v1.1.0 tag pushed to trigger first release.
</objective>

<execution_context>
@/Users/pedf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pedf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-distribution-documentation/05-RESEARCH.md
@README.md
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README and release workflow with installation documentation</name>
  <files>README.md, .github/workflows/release.yml</files>
  <action>
Update README.md Installation section. Replace the existing Installation section (lines 16-38 approximately) with a comprehensive version. Keep all other sections (header, Features, Usage, Development, License) unchanged.

New Installation section structure:
1. **Download** subsection - Link to GitHub Releases page (https://github.com/bacilo/pomodoro-mac/releases)
2. **Standard Installation** subsection - Numbered steps: download zip, extract, move to /Applications, right-click Open. Include callout for Sequoia users directing them to next section.
3. **macOS 15.1+ Sequoia Installation** subsection - Steps: extract and move app, open Terminal, run `xattr -cr /Applications/PomodoroApp.app`, go to System Settings > Privacy & Security, click Open Anyway. Include "What does xattr -cr do?" explanation (removes security flags macOS adds to downloads, does not modify app code). Include "Why is this needed?" explanation (Sequoia 15.1+ removed traditional right-click bypass).
4. **Verify Download (Optional)** subsection - Download checksums.txt, run `shasum -a 256 -c checksums.txt`, expected output.
5. **Build from Source** subsection - Keep existing content (git clone, xcodebuild, open Xcode).
6. **Why isn't this app signed?** subsection - Brief explanation: free open-source project, no Apple Developer account ($99/year), ad-hoc signing for Apple Silicon compatibility, common for open-source macOS apps.

Use the research code example as reference but adapt to match existing README tone (concise, professional).

Also update `.github/workflows/release.yml`: Change `xattr -r -d com.apple.quarantine /Applications/PomodoroApp.app` to `xattr -cr /Applications/PomodoroApp.app` in the release body template (line 136 area). This ensures consistency between README and release notes per DOCS-02 requirement.
  </action>
  <verify>
1. `grep -c "xattr -cr" README.md` returns at least 1
2. `grep -c "xattr -cr" .github/workflows/release.yml` returns at least 1
3. `grep -c "xattr -r -d" .github/workflows/release.yml` returns 0 (old form removed)
4. README.md contains "Sequoia" section
5. README.md contains "Verify Download" section
6. README.md contains "Build from Source" section
7. README.md contains "Why isn't this app signed" section
  </verify>
  <done>
README.md has comprehensive Installation section with standard install, Sequoia workaround (xattr -cr), checksum verification, build from source, and signing explanation. Release workflow uses consistent xattr -cr command. Both user-facing surfaces use the same command.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create v1.1.0 tag and trigger first release</name>
  <files>README.md</files>
  <action>
After Task 1 changes are committed, create and push the v1.1.0 tag to trigger the GitHub Actions release workflow.

Steps:
1. Ensure all changes from Task 1 are committed
2. Create annotated tag: `git tag -a v1.1.0 -m "Release v1.1.0 - First automated release with universal binary support"`
3. Push the tag: `git push origin v1.1.0`
4. Verify workflow was triggered by checking: `gh run list --workflow=release.yml --limit=1`

Note: The release workflow (from Phase 3) will automatically build, package, and create the GitHub Release with the v1.1.0 zip and checksums.
  </action>
  <verify>
1. `git tag -l v1.1.0` shows the tag exists
2. `gh run list --workflow=release.yml --limit=1` shows a run in progress or completed
3. After workflow completes: `gh release view v1.1.0` shows the release with assets
  </verify>
  <done>
v1.1.0 tag exists and has been pushed. GitHub Actions workflow has been triggered. Release is created with PomodoroApp-v1.1.0.zip and checksums.txt as downloadable assets.
  </done>
</task>

</tasks>

<verification>
1. README.md Installation section covers: download link, standard install steps, Sequoia xattr -cr workaround, checksum verification, build from source, signing explanation
2. xattr -cr is used consistently in README.md and .github/workflows/release.yml (no xattr -r -d)
3. v1.1.0 release exists on GitHub with downloadable zip and checksums
4. Release page links work and assets are downloadable
</verification>

<success_criteria>
- README.md has comprehensive Installation section satisfying DOCS-01 and DOCS-02
- xattr -cr command documented with explanation in README (DOCS-02)
- Release workflow and README use consistent xattr -cr command
- v1.1.0 release published and downloadable from GitHub Releases
</success_criteria>

<output>
After completion, create `.planning/phases/05-distribution-documentation/05-01-SUMMARY.md`
</output>
